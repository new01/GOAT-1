---
phase: 02-wallet-connection
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .env.example
  - .env.local
autonomous: false
requirements: []

must_haves:
  truths:
    - ".env.example documents x402 API credentials for future phases"
    - "Visual verification confirms wallet connection flow works end-to-end"
  artifacts:
    - path: ".env.example"
      provides: "Updated environment variable documentation including x402 credentials"
      contains: "GOATX402"
---

<objective>
Add x402 API credential environment variables for future phases (3 and 4) and perform visual verification of the complete wallet connection flow.

Purpose: Ensure the x402 credentials provided by the user are stored in the right place, and verify the complete Phase 2 experience works visually.
Output: Updated .env files with x402 credentials and human-verified wallet connection flow.
</objective>

<execution_context>
@/Users/wesleyw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wesleyw/.claude/get-shit-done/templates/summary.md
@/Users/wesleyw/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wallet-connection/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add x402 API credentials to environment files</name>
  <files>.env.example, .env.local</files>
  <action>
    **Step 1: Update .env.example**
    Add x402 credential placeholders (these are for Phase 3/4 but the user provided them now):
    ```
    # x402 Payment API (Phase 3-4)
    GOATX402_API_URL=https://x402-api-lx58aabp0r.testnet3.goat.network
    GOATX402_MERCHANT_ID=your_merchant_id
    GOATX402_API_KEY=your_api_key
    GOATX402_API_SECRET=your_api_secret
    ```

    **Step 2: Update .env.local**
    Add the actual credentials the user provided:
    ```
    # x402 Payment API
    GOATX402_API_URL=https://x402-api-lx58aabp0r.testnet3.goat.network
    GOATX402_MERCHANT_ID=visibit
    GOATX402_API_KEY=zV9qKj-Av7GWruTuUVfvI89X8TTf0tMyN9dQRd2g6Eg=
    GOATX402_API_SECRET=
    ```
    Note: API_SECRET was not provided (user noted "see note below"). Leave empty — user will fill in.
  </action>
  <verify>
    <automated>grep "GOATX402" .env.example && echo "---" && grep "GOATX402" .env.local</automated>
  </verify>
  <done>
    - .env.example documents all x402 credential vars
    - .env.local has actual values (except secret which user will fill)
    - Credentials are ready for Phase 3-4 x402 integration
  </done>
</task>

<task type="human-verify">
  <name>Task 2: Verify complete wallet connection flow</name>
  <files></files>
  <action>
    Present the verification checklist to the user. The dev server should already be running from Plan 02-01.

    **Verification steps:**

    **Test 1 — Initial state (no wallet):**
    1. Open http://localhost:3000
    2. Verify: Step 1 shows "Connect your wallet to GOAT Testnet3" with RainbowKit ConnectButton
    3. Verify: Header shows "Not Connected" on right and "0:00" timer
    4. Verify: "No wallet?" section is expandable and shows MetaMask install guide

    **Test 2 — Wallet connection:**
    1. Click "Connect Wallet" button
    2. Verify: RainbowKit modal appears with wallet options
    3. Connect with MetaMask (or preferred wallet)
    4. Verify: Header updates to show wallet address and network name
    5. Verify: Timer starts counting

    **Test 3 — Network handling:**
    1. If already on GOAT Testnet3: verify green "Connected" state with address and balances
    2. If on wrong network: verify amber warning with "Switch to GOAT Testnet3" button
    3. Click switch button: verify network switches successfully

    **Test 4 — Balances:**
    1. Verify: BTC, USDC, and USDT balances displayed
    2. Verify: Any zero balances show faucet link
    3. Verify: Balance formatting looks reasonable (not raw wei values)

    **Test 5 — Step completion:**
    1. Click "Continue →" button
    2. Verify: Step 1 shows checkmark in sidebar
    3. Verify: Step 2 becomes current
    4. Verify: Smooth animation transition
    5. Click Step 1 in sidebar: verify shows completed summary with address/balances

    **Test 6 — Error states:**
    1. (Optional) Reject a connection attempt in wallet and verify error message appears
    2. (Optional) Reject a network switch and verify error message appears
  </action>
  <verify>
    Human visual verification — user responds "approved" or describes issues.
  </verify>
  <done>
    - All 6 test groups verified by human
    - Wallet connection flow works end-to-end
    - Error messages are clear and actionable
  </done>
</task>

</tasks>

<verification>
1. .env.example has GOATX402_API_URL, GOATX402_MERCHANT_ID, GOATX402_API_KEY, GOATX402_API_SECRET
2. .env.local has actual credential values (except secret)
3. Human verified all wallet connection test groups
</verification>

<success_criteria>
- x402 credentials stored and ready for Phase 3-4
- Complete wallet connection flow verified by human
- No visual or functional regressions from Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-wallet-connection/02-02-SUMMARY.md`
</output>
