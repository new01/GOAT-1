---
phase: 02-wallet-connection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/wizard/steps/step-connect-wallet.tsx
  - components/wizard/steps/index.ts
  - components/wizard/wizard-header.tsx
  - components/wizard/wizard-shell.tsx
  - hooks/use-wizard.ts
  - hooks/use-timer.ts
  - lib/constants.ts
  - lib/abi.ts
  - app/globals.css
autonomous: true
requirements:
  - WALL-01
  - WALL-02
  - WALL-03
  - WALL-04
  - WALL-05
  - WIZ-04

must_haves:
  truths:
    - "User can connect wallet via RainbowKit modal (MetaMask, Rabby, Coinbase Wallet, WalletConnect)"
    - "Connected wallet address and network name are displayed in the header and step content"
    - "User on wrong network sees a prompt and can one-click switch to GOAT Testnet3 (chain ID 48816)"
    - "User without a wallet sees a No wallet? guide with MetaMask install steps and GOAT Testnet3 config"
    - "User can see BTC, USDC, and USDT balances on GOAT Testnet3 with faucet link when any balance is zero"
    - "Inline error messages appear with actionable fix instructions when wallet connection or network switching fails"
    - "Timer in header starts counting from wallet connect and persists across refreshes via Convex startedAt"
    - "Wallet address is wired through to useWizard for Convex persistence of step progress"
  artifacts:
    - path: "components/wizard/steps/step-connect-wallet.tsx"
      provides: "Real Connect Wallet step replacing placeholder for step 0"
      contains: "StepConnectWallet"
    - path: "hooks/use-timer.ts"
      provides: "Elapsed timer hook using startedAt timestamp"
      exports: ["useTimer"]
    - path: "lib/abi.ts"
      provides: "Minimal ERC-20 ABI for balance reading"
      exports: ["erc20BalanceOfABI"]
  key_links:
    - from: "components/wizard/steps/index.ts"
      to: "components/wizard/steps/step-connect-wallet.tsx"
      via: "registers StepConnectWallet for step 0"
      pattern: "import.*StepConnectWallet"
    - from: "components/wizard/wizard-shell.tsx"
      to: "hooks/use-wizard.ts"
      via: "passes wallet address for Convex persistence"
      pattern: "useWizard.*address"
    - from: "components/wizard/wizard-header.tsx"
      to: "hooks/use-timer.ts"
      via: "displays elapsed time from startedAt"
      pattern: "useTimer"
---

<objective>
Build the complete Connect Wallet step (Step 1 of the wizard) with RainbowKit wallet connection, network detection and one-click switching to GOAT Testnet3, token balance display (BTC/USDC/USDT) with faucet links, a "No wallet?" guide, inline error messages, and header integration showing wallet address, network, and elapsed timer.

Purpose: Transform step 0 from a placeholder into a fully functional wallet onboarding experience. After this plan, users can connect their wallet, land on the correct network, see their balances, and proceed to Step 2.
Output: A working Connect Wallet step that satisfies WALL-01 through WALL-05 and WIZ-04, with wallet state wired to the wizard for Convex persistence.
</objective>

<execution_context>
@/Users/wesleyw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wesleyw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/chains.ts:
```typescript
export const goatTestnet3: Chain; // viem chain definition, id: 48816
```

From lib/constants.ts:
```typescript
export const GOAT_TESTNET3: {
  chainId: 48816;
  chainIdHex: "0xBEB0";
  rpc: string;
  explorer: string;
  faucet: string;
  tokens: { USDC: string; USDT: string };
};
```

From types/wizard.ts:
```typescript
export enum StepStatus { COMPLETED, CURRENT, LOCKED }
export interface StepDefinition { number: number; name: string; description: string; icon: string; }
export interface WizardState { currentStep; completedSteps; isLoading; getStepStatus; goToStep; completeCurrentStep; canNavigateToStep; }
```

From hooks/use-wizard.ts:
```typescript
export function useWizard(walletAddress?: string): WizardState
// Dual mode: local state when no wallet, Convex persistence when wallet provided
```

From app/providers.tsx:
```typescript
// Provider stack: WagmiProvider > QueryClientProvider > RainbowKitProvider > ConvexWrapper
```

Wagmi v3 hooks (from wagmi package):
- useAccount() → { address, isConnected, chain, connector }
- useSwitchChain() → { switchChain, isPending, error }
- useBalance({ address }) → { data: { formatted, symbol, value }, isLoading }
- useReadContract({ address, abi, functionName, args }) → { data, isLoading }

RainbowKit v2:
- ConnectButton → renders connect/disconnect with chain/address display
- ConnectButton.Custom → render prop for custom UI
- useConnectModal() → { openConnectModal }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Connect Wallet step component with full wallet connection UI</name>
  <files>
    components/wizard/steps/step-connect-wallet.tsx,
    components/wizard/steps/index.ts,
    components/wizard/wizard-header.tsx,
    components/wizard/wizard-shell.tsx,
    hooks/use-wizard.ts,
    hooks/use-timer.ts,
    lib/abi.ts,
    lib/constants.ts
  </files>
  <action>
    **Step 1: Create minimal ERC-20 ABI for balance reading**
    Create `lib/abi.ts`:
    ```typescript
    export const erc20BalanceOfABI = [
      {
        name: "balanceOf",
        type: "function",
        stateMutability: "view",
        inputs: [{ name: "account", type: "address" }],
        outputs: [{ name: "balance", type: "uint256" }],
      },
      {
        name: "decimals",
        type: "function",
        stateMutability: "view",
        inputs: [],
        outputs: [{ name: "", type: "uint8" }],
      },
    ] as const;
    ```

    **Step 2: Add faucet URL to constants if not already there**
    Verify `lib/constants.ts` has the faucet URL. It should already have `faucet: "https://bridge.testnet3.goat.network/faucet"`. If not, add it.

    **Step 3: Create useTimer hook**
    Create `hooks/use-timer.ts` with `"use client"`:
    - Takes `startedAt: number | null | undefined` (epoch ms timestamp)
    - Returns `{ elapsed: string, seconds: number }`
    - Uses `setInterval(1000)` to update every second
    - Formats as `M:SS` (e.g., "0:00", "1:47", "12:30")
    - Returns "0:00" when startedAt is null/undefined
    - Cleans up interval on unmount

    **Step 4: Update useWizard hook to expose startedAt and setStartedAt**
    Edit `hooks/use-wizard.ts`:
    - Add `startedAt: number | null` to the WizardState interface in types/wizard.ts
    - In local mode: track startedAt in useState, initialize to null
    - In Convex mode: read startedAt from the builder record
    - Add `setStartedAt(timestamp: number)` method to WizardState
    - When setStartedAt is called in Convex mode, call upsertBuilder mutation to persist it
    - Export the updated interface

    **Step 5: Create StepConnectWallet component**
    Create `components/wizard/steps/step-connect-wallet.tsx` with `"use client"`:

    Props: same as StepPlaceholder: `{ step: StepDefinition, status: StepStatus, onComplete: () => void }`

    **UI States (render conditionally):**

    **State A: Not Connected**
    - Large "Connect your wallet to GOAT Testnet3" heading
    - RainbowKit `ConnectButton` component (default styling)
    - Brief description: "Connect an EVM wallet to get started. MetaMask, Rabby, Coinbase Wallet, and WalletConnect are supported."
    - Collapsible "No wallet?" section (use a simple disclosure toggle):
      - "1. Install MetaMask from metamask.io/download"
      - "2. Create or import a wallet"
      - "3. Add GOAT Testnet3 network:" with config details:
        - Network Name: GOAT Testnet3
        - RPC URL: https://rpc.testnet3.goat.network
        - Chain ID: 48816
        - Symbol: BTC
        - Explorer: https://explorer.testnet3.goat.network
      - "4. Get test tokens from the faucet" with faucet link
    - Subtle error display area (hidden when no error)

    **State B: Connected, Wrong Network**
    - Show connected address (truncated: 0x1234...5678)
    - Yellow/amber warning box: "You're connected to {chainName}. Switch to GOAT Testnet3 to continue."
    - Prominent "Switch to GOAT Testnet3" button using `useSwitchChain({ chainId: 48816 })`
    - If switch fails, show inline error: "Failed to switch network. Try switching manually in your wallet."

    **State C: Connected, Correct Network**
    - Green success indicator: "Connected to GOAT Testnet3"
    - Display wallet address (full, with copy button would be nice but not required)
    - **Balance section** (use wagmi hooks):
      - BTC balance: `useBalance({ address })` — native currency
      - USDC balance: `useReadContract({ address: GOAT_TESTNET3.tokens.USDC, abi: erc20BalanceOfABI, functionName: "balanceOf", args: [address] })`
      - USDT balance: `useReadContract({ address: GOAT_TESTNET3.tokens.USDT, abi: erc20BalanceOfABI, functionName: "balanceOf", args: [address] })`
      - Format: "{amount} {symbol}" for each
      - If any balance is zero: show inline "Get test tokens →" link to faucet URL
      - For ERC-20 tokens, also call `decimals()` to format correctly (likely 6 for USDC/USDT), or hardcode 6 if the call is unreliable on testnet
    - "Continue →" button that calls `onComplete()` — only enabled when connected to correct network
    - This button also calls `setStartedAt(Date.now())` if startedAt is null (first connection triggers timer)

    **State D: Completed (read-only)**
    - Green checkmark with "Wallet Connected"
    - Show address and network
    - Show balances (read-only)

    **Error handling (WIZ-04):**
    - Connection errors: "Failed to connect. Check that your wallet extension is unlocked and try again."
    - Network switch errors: "Could not switch network automatically. Open your wallet and switch to GOAT Testnet3 (Chain ID 48816) manually."
    - Balance read errors: Show "—" for balances that fail to load, no error toast
    - Use a local state for error messages, clear on retry

    Use shadcn Card for the main container. Use Tailwind for all styling.
    Keep the component self-contained — all wagmi hooks are called directly since it's inside the provider tree.

    **Step 6: Update step registry**
    Edit `components/wizard/steps/index.ts`:
    - Import `StepConnectWallet` from `./step-connect-wallet`
    - Change step 0 mapping from `StepPlaceholder` to `StepConnectWallet`
    - Keep steps 1-4 as `StepPlaceholder`

    **Step 7: Update WizardHeader to show real wallet info and timer**
    Edit `components/wizard/wizard-header.tsx`:
    - Accept props: `walletAddress?: string`, `chainName?: string`, `startedAt?: number | null`
    - Left: "Hello GOAT" (unchanged)
    - Center: Use `useTimer(startedAt)` to display elapsed time (replaces "0:00" placeholder)
    - Right: If walletAddress, show truncated address (0x1234...5678) + chain name. If not, show "Not Connected"
    - Keep the same layout and styling

    **Step 8: Update WizardShell to wire wallet state**
    Edit `components/wizard/wizard-shell.tsx`:
    - Import `useAccount` from wagmi
    - Get `{ address, chain }` from useAccount()
    - Pass `address` as string to `useWizard(address)` (already supports this)
    - Pass `walletAddress`, `chainName: chain?.name`, and `startedAt` to WizardHeader
    - The wallet address will now flow through to Convex for persistence
  </action>
  <verify>
    <automated>cd /Users/wesleyw/Documents/GitHub/GOAT-1 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Step 0 renders a real Connect Wallet UI instead of placeholder
    - RainbowKit ConnectButton opens wallet modal with MetaMask, Rabby, Coinbase, WalletConnect
    - Wrong network shows amber warning with one-click switch button
    - Correct network shows green success with address, balances (BTC/USDC/USDT), faucet links
    - "No wallet?" guide shows MetaMask install steps and GOAT Testnet3 config
    - Inline error messages for connection and network switch failures
    - Header shows real wallet address, network name, and running timer
    - Wallet address wired to useWizard for Convex step persistence
    - Timer starts on first wallet connection and persists via startedAt
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. Step 0 shows RainbowKit ConnectButton when no wallet connected
3. Connecting wallet shows address and network in header
4. Wrong network shows switch prompt with one-click button
5. Correct network shows BTC, USDC, USDT balances
6. Zero balances show faucet link
7. "No wallet?" section expandable with MetaMask install guide
8. Error messages appear inline with actionable instructions
9. Timer starts counting on wallet connect
10. Timer persists across page refreshes (Convex startedAt)
</verification>

<success_criteria>
- User can go from "not connected" to "connected on GOAT Testnet3 with balances visible" in under 30 seconds
- Every error state has an actionable fix instruction
- The step auto-advances to show "Continue →" when requirements are met
- Wallet address flows to Convex for persistent step tracking
</success_criteria>

<output>
After completion, create `.planning/phases/02-wallet-connection/02-01-SUMMARY.md`
</output>
